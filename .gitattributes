# Ensure that Git will always convert line endings to LF on checkout
* text=auto
# * text eol=lf # this affected pngs and such...

# specify binary files so EOL won't be changed
*.webp binary
*.png binary
*.jpg binary